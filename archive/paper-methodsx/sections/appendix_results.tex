% NOTE (rewrite): the table below (tables/appendix_per_image.tex) still holds the
% old 23-image per-image wire results. Regenerate it from the current 134-image
% benchmark (scripts/gen_join_report.py / the expanded benchmark) before submission
% so it matches the wire F1 = 0.9755 reported in the Method validation.
The per-image wire-extraction results for the 134-image benchmark are listed in
Table~\ref{tab:appendixresults}. These values are generated directly from the
repository benchmark output and are not entered manually.

\begin{table}[H]
\centering
\caption{Per-image wire-extraction results on the 134-image benchmark.}
\input{tables/appendix_per_image}
\label{tab:appendixresults}
\end{table}
