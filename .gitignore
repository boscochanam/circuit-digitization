__pycache__/
*.pyc
*.egg-info/
output/
output_run_*/
roboflow_test/
roboflow_test2/
Netlist Generator/
Database/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# UI
ui/node_modules/
ui/.next/
ui/out/

# mkdocs build output
site/

# Misc
visualize_results.py
datasets/
data/
!docs/experiments/data/
line_detection/
output_run_24_morph_thin/
output_run_26_hybrid2/
.env

# Model weights (too large for git)
models/
labels_few_annot/
# Broken symlink to a dataset path outside the repo (see roboflow_test2 below)
/labels_few_annot

# Claude session temp archives (local only, multi-GB); keep the README pointer
docs/research/session-artifacts/*/

# LaTeX build artifacts
paper/ieee-paper/*.aux
paper/ieee-paper/*.log
paper/ieee-paper/*.out

# Local-only repro scaffolding (symlinks to dataset paths outside the repo)
roboflow_test2
ground_truth/local_eval/
# Abandoned MIMO VLM experiment (scratch)
docs/research/experiments/vlm_mimo_v25*.json

# LaTeX build byproducts
paper/ieee-paper/*.bbl
paper/ieee-paper/*.blg

# Overleaf submission bundles and local-only paper artifacts (do not commit)
/paper-access-overleaf-full.zip
/paper-access-overleaf.zip
paper/paper-access-overleaf-upload.zip
paper/ieee-paper/paper-access-LOCAL-verified.pdf
# Build output; 20 revisions of it were purged from history (70 MB).
# Regenerate with: pdflatex paper-build.tex
paper/ieee-paper/paper-build.pdf

# Editor/assistant tooling config. Local-only, never committed. The durable
# content that used to live in AGENTS.md is now docs/pipeline-internals.md
# (engineering contract) and docs/benchmark-provenance.md (empirical records).
AGENTS.md
CLAUDE.md
**/AGENTS.md
**/CLAUDE.md
.hermes-agent
.omo/
.claude/
# Original (pre-processing) author headshots
paper/ieee-paper/figures/authors/.orig/
