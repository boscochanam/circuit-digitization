# Coauthor revision source bundle

This is a source-review handoff for `revision/access-2026-33821-astra-exec`, starting at `cefe77b`. The fork at `10c1b926d619070143a81eaf9887ff4990916da9` was fetched from `tkprnv` and selectively reconciled file by file. The unrelated histories were not merged or rebased.

## Contents

- `../paper-access.tex`: IEEE Access submission source.
- `../paper-build.tex`: IEEEtran source with synchronized scientific prose and tables.
- `RESPONSE_TO_REVIEWERS.md`: twelve concerns and factual responses retained from the newer revision, to be aligned with the local IEEE template in T4.
- `REVIEW_CHANGES.md`: prior revision record followed by this session's T1 decision ledger, task records and source checks.
- `manuscript_changes.diff`: **historical fork artifact**, preserved byte for byte from `10c1b92`. It records the fork's `9bbe9e9`-based changes, including claims rejected during this port. It is not the final-source diff, a patch to apply here, or proof of the identity of the manuscript reviewed by IEEE.

## Review boundaries

The manuscript distinguishes inferred pin-to-node output, evaluated component-pair connectivity and illustrative export. The VLM diagnostic gives both methods annotated component boxes. Provisional autonomous scores, completion-masking explanations and blanket device-export exclusions from the fork are not imported into the live response or manuscript.

The existing revision experiments, figure assets, table values, corrected component-first assignment and author fields take precedence over the fork. Access currently consumes three concept PDFs while IEEEtran consumes TikZ sources; this pre-existing dependency difference and the stale Access pipeline graphic remain recorded for later figure reconciliation. Equal scientific prose does not certify equal figure contents.

No clean, highlighted or response PDF is produced or certified by this handoff. The fork README's claims of a supplied reviewed 12-page PDF and bundled template support do not describe this checkout and are not retained. Builds, reruns, pushes, email and author/funding/bio edits are outside this session. Final pagination, reviewed-baseline confirmation, package preparation and author approvals remain later work.
