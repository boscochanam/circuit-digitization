# Copy to .env before: docker compose up --build
#
# Ground-truth images + labels (for "GT Labels" in the UI)
GT_LABELS_PATH=/home/claw/workspace/ground_truth/labels_few_annot
#
# If port 8000 is already in use locally, change the host port:
# BACKEND_PORT=8008
# FRONTEND_PORT=4200
