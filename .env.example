# Dataset configuration file
DATASETS_YAML=wire_detection/config/datasets.yaml

# Backend API
BACKEND_PORT=8000
FRONTEND_PORT=3000

# Frontend API URL (the address the browser uses to reach the backend)
NEXT_PUBLIC_API_URL=http://localhost:8000
